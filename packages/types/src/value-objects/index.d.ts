/**
 * Placeholders for future rich Value Objects.
 * Currently defined as primitive aliases/simple structures to avoid over-engineering,
 * but reserved for future domain validation encapsulation.
 */
export type UserId = string;
export type IssueId = string;
export type TenantId = string;
export type Email = string;
export interface GeoLocation {
    latitude: number;
    longitude: number;
}
//# sourceMappingURL=index.d.ts.map