# Development Guide

## Prerequisites
- Node.js >= 18
- MongoDB (local or Atlas)
- Redis

## Running Locally
1. Install dependencies: `npm install`
2. Set up `.env` files based on `.env.example`.
3. Start the Turborepo dev server: `npm run dev`

This will start the frontend on port 3000 and the API on port 5000.
